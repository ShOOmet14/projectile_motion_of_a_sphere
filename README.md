# To do

1. Add more space for parameters panel
1. refractoring of the code, join similar files, make the project have as little files as it needs. if ok don't change anything
1. make nice looking windows using styling in css
1. take wind into consideration
1. add vectors on plots for wind (an arrow which points in wind direction)
1. add possibility to change wind strength and angle
1. add vectors show vectors option inside settings part of parameters, as checkbox, if enabled it will show vectors for wind (on plots and video playback) and velocity (on video playback) so we can see how force changes during flight.
1. make latex file to pdf explaining all physics logic in here, explaining algorithms and the whole project
1. make good readme

CSV contains interpolated point for ground hit.
